{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Read",
      "Grep",
      "Glob",
      "Bash(git status *)",
      "Bash(git diff *)",
      "Bash(git log *)",
      "Bash([fill in: install command, e.g. npm install] *)",
      "Bash([fill in: test command, e.g. npm test] *)",
      "Bash([fill in: lint/typecheck command, e.g. npm run lint] *)",
      "Bash([fill in: build command, e.g. npm run build] *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"[PLACEHOLDER: fill in the stack-specific format/lint-on-save command, e.g. npx prettier --write ., ruff format ., gofmt -w .]\""
          }
        ]
      }
    ]
  }
}
